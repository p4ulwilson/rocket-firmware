# Rocket Routers Firmware A v2.5.2 - package additions
# Append to .config in ~/src/openwrt then run: make defconfig
#
# Build delta v2.5.1 -> v2.5.2:
#   - REMOVED luci-app-qmodem-monitor (broken init script + AT port contention)
#   - REMOVED luci-app-qmodem-mwan    (its uci-defaults reactivated mwan3
#                                      after our gate disabled it - blackholes)
#   - ADDED   wpad-mesh-mbedtls       (replaces default wpad to give SAE +
#                                      802.11s mesh in one binary)
#   - KEPT    iperf3                  (server is now baked-in via uci-defaults)
#   - KEPT    everything else from v2.5.1

# ============================================================================
# v2.4 baseline (unchanged)
# ============================================================================

# Multi-WAN failover/load-balance
CONFIG_PACKAGE_mwan3=y
CONFIG_PACKAGE_luci-app-mwan3=y

# SQM / cake bufferbloat control
CONFIG_PACKAGE_luci-app-sqm=y
CONFIG_PACKAGE_sqm-scripts=y
CONFIG_PACKAGE_kmod-sched-cake=y

# Statistics + graphs
CONFIG_PACKAGE_luci-app-statistics=y
CONFIG_PACKAGE_collectd-mod-cpu=y
CONFIG_PACKAGE_collectd-mod-memory=y
CONFIG_PACKAGE_collectd-mod-interface=y
CONFIG_PACKAGE_collectd-mod-iwinfo=y
CONFIG_PACKAGE_collectd-mod-ping=y

# Bandwidth monitor
CONFIG_PACKAGE_luci-app-nlbwmon=y

# ModemManager (kept installed but disabled at runtime - Q-modem owns AT)
CONFIG_PACKAGE_modemmanager=y
CONFIG_PACKAGE_luci-proto-modemmanager=y
CONFIG_PACKAGE_libqmi=y
CONFIG_PACKAGE_libmbim=y
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y
CONFIG_PACKAGE_kmod-usb-net-cdc-mbim=y

# Software flow offloading kernel side (nftables)
CONFIG_PACKAGE_kmod-nft-offload=y

# ============================================================================
# v2.5 baseline (unchanged from v2.5.1)
# ============================================================================

# WireGuard VPN
CONFIG_PACKAGE_kmod-wireguard=y
CONFIG_PACKAGE_wireguard-tools=y
CONFIG_PACKAGE_luci-app-wireguard=y

# banIP - threat-feed firewall (default disabled, switchable in LuCI)
CONFIG_PACKAGE_banip=y
CONFIG_PACKAGE_luci-app-banip=y

# Diagnostics
CONFIG_PACKAGE_mtr=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_luci-app-speedtest-netperf=y

# ============================================================================
# v2.5.2: Q-Modem GUI suite (TRIMMED)
# Keep:    luci-app-qmodem-next     (Modems page - works)
# Keep:    luci-app-qmodem-sms      (SMS page - works)
# DROPPED: luci-app-qmodem-monitor  (init script broken: basename error)
# DROPPED: luci-app-qmodem-mwan     (its 99-setup-mwan re-enables mwan3
#                                    AFTER our gate disables it -> blackhole)
# ============================================================================
CONFIG_PACKAGE_luci-app-qmodem-next=y
CONFIG_PACKAGE_luci-app-qmodem-sms=y
# CONFIG_PACKAGE_luci-app-qmodem-monitor is not set
# CONFIG_PACKAGE_luci-app-qmodem-mwan is not set

# ============================================================================
# v2.5.2 NEW: 802.11s mesh + 802.11r FT (wpad-mesh-mbedtls replaces default wpad)
# Conflicts with default 'wpad-basic-mbedtls'; defconfig will swap them.
# ============================================================================
# CONFIG_PACKAGE_wpad-basic-mbedtls is not set
# CONFIG_PACKAGE_wpad-basic is not set
CONFIG_PACKAGE_wpad-mesh-mbedtls=y

# ============================================================================
# v2.5 REMOVED (still removed in v2.5.2): Attended Sysupgrade
# Reason: would wipe customisations on upgrade.
# ============================================================================
# CONFIG_PACKAGE_luci-app-attendedsysupgrade is not set
# CONFIG_PACKAGE_attendedsysupgrade-common is not set
